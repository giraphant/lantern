# ========================================
# GRVT Configuration
# ========================================
# 从 GRVT 账户设置中获取
GRVT_TRADING_ACCOUNT_ID=
GRVT_PRIVATE_KEY=
GRVT_API_KEY=
GRVT_ENVIRONMENT=prod

# ========================================
# Lighter Configuration
# ========================================
# LIGHTER_ACCOUNT_INDEX: 你的 Lighter 账户索引
# 获取方法: https://mainnet.zklighter.elliot.ai/api/v1/account?by=l1_address&value=YOUR_WALLET_ADDRESS
LIGHTER_ACCOUNT_INDEX=

# LIGHTER_API_KEY_INDEX: Lighter API Key 的索引（通常是4）
LIGHTER_API_KEY_INDEX=4

# LIGHTER_API_PRIVATE_KEY: Lighter API Key 的私钥（完整的hex字符串，无0x前缀）
# 这是 API Key 本身，用于签名和认证 API 请求
LIGHTER_API_PRIVATE_KEY=

# ========================================
# Trading Parameters
# ========================================
# 交易参数 - 这些参数控制机器人的交易行为

# EXCHANGE: 交易所选择 (必填)
# 可选值: grvt, apex, backpack, extended
EXCHANGE=grvt

# TICKER: 交易对 (必填)
# 例如: BTC, ETH, HYPE
TICKER=HYPE

# SIZE: 每笔订单的数量 (必填)
# 例如: BTC用0.05, ETH用0.5, HYPE用10
SIZE=10

# ========================================
# Cycle Mode Parameters (循环模式参数)
# ========================================
# 循环模式: 累积仓位 → 持有 → 平仓 → 重复

# BUILD_UP_ITERATIONS: 累积阶段的订单数量 (可选)
# 例如: 30 表示累积30笔订单后进入持有阶段
# 默认: 等于 ITERATIONS
BUILD_UP_ITERATIONS=30

# HOLD_TIME: 持有时间（秒） (可选)
# 例如: 180 = 3分钟, 1800 = 30分钟, 3600 = 1小时
# 默认: 0 (不持有，传统模式)
HOLD_TIME=180

# CYCLES: 循环次数 (可选)
# 例如: 24 表示运行24个周期, 999999 表示无限循环
# 默认: 1
CYCLES=999999

# DIRECTION: Lighter的交易方向 (可选)
# long - Lighter做多(买入), GRVT做空对冲 (适合Lighter正费率)
# short - Lighter做空(卖出), GRVT做多对冲 (适合Lighter负费率)
# random - 每个周期随机选择方向
# 默认: long
DIRECTION=long

# ========================================
# Safety Parameters (安全参数) - IMPORTANT!
# ========================================

# MAX_POSITION: 单边最大仓位限制
# 超过此值会触发安全停止
# 默认: 10
MAX_POSITION=10

# MAX_OPEN_ORDERS: 最大允许挂单数
# 超过此值会取消所有挂单
# 默认: 1
MAX_OPEN_ORDERS=1

# ORDER_TIMEOUT: 订单超时时间（秒）
# 超时后取消订单
# 默认: 30
ORDER_TIMEOUT=30

# MAX_RETRIES: 订单最大重试次数
# 失败后重试的次数
# 默认: 3
MAX_RETRIES=3

# ========================================
# Advanced Parameters (高级参数)
# ========================================

# REBALANCE_TOLERANCE: 最大允许仓位偏差
# 当两个交易所的仓位差异超过此值时停止交易
# 默认: 0.15
REBALANCE_TOLERANCE=0.15
